[Unit]
Description=Daily Mill Mate database backup

[Timer]
# 02:30 server time (UTC on Ubuntu by default). Persistent=true runs a missed
# backup as soon as the server is back up.
OnCalendar=*-*-* 02:30:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
